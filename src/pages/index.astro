---
import BaseLayout from '../layouts/BaseLayout.astro';

// Récupérer les dernières actualités
const allNewsModules = import.meta.glob('../content/actualites/*.md');
const allNews = await Promise.all(
  Object.entries(allNewsModules).map(async ([path, module]) => {
    const mod = await (module as any)();
    return {
      ...mod,
      file: path
    };
  })
);
const recentNews = allNews
  .sort((a, b) => new Date(b.frontmatter.date).getTime() - new Date(a.frontmatter.date).getTime())
  .slice(0, 3);
---

<BaseLayout title="Accueil" description="Étoile Cycliste Ouvrière de Firminy - Découvrez notre club de cyclisme">
  <!-- Hero Section -->
  <section class="relative h-[60vh] sm:h-80 md:h-96 flex items-center justify-center overflow-hidden">
    <!-- Hero Background Image -->
    <div class="absolute inset-0 z-0">
      <img 
        src="/images/GroupRide.png" 
        alt="Groupe de cyclistes ECOF" 
        class="w-full h-full object-cover hero-image sm:object-center md:object-center"
        style="object-position: 50% 20%;"
      />
      <!-- Overlay plus sombre pour mobile pour améliorer la lisibilité -->
      <div class="absolute inset-0 bg-black/40 sm:bg-black/30"></div>
    </div>
    
    <!-- Hero Content -->
    <div class="relative z-10 text-center text-white px-4">
      <h1 class="text-3xl sm:text-4xl md:text-6xl font-bold mb-4 drop-shadow-lg">
        Bienvenue à l'E.C.O.F.
      </h1>
      <p class="text-lg sm:text-xl md:text-2xl font-light drop-shadow-md">
        Étoile Cycliste Ouvrière de Firminy
      </p>
    </div>
    
    <!-- Scroll indicator -->
    <div class="absolute bottom-4 sm:bottom-8 left-1/2 transform -translate-x-1/2 text-white animate-bounce">
      <svg class="w-5 h-5 sm:w-6 sm:h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </section>

  <style>
    /* Optimisation de l'image hero pour différentes tailles d'écran */
    @media (max-width: 640px) {
      /* Mobile : centrer l'image et ajuster la position */
      .hero-image {
        object-position: 50% 25% !important;
      }
    }
    
    @media (min-width: 641px) and (max-width: 1024px) {
      /* Tablette : position légèrement ajustée */
      .hero-image {
        object-position: 50% 30% !important;
      }
    }
    
    @media (min-width: 1025px) {
      /* Desktop : position optimale */
      .hero-image {
        object-position: 50% 30% !important;
      }
    }
  </style>

  <!-- Section En bref -->
  <section class="py-12 sm:py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">En bref</h2>
        <p class="text-lg text-gray-600">Les dernières nouvelles de l'E.C.O.F.</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {recentNews.map((news) => {
          const slug = news.file.split('/').pop()?.replace('.md', '') || '';
          return (
            <article class="bg-gray-50 rounded-2xl overflow-hidden hover:shadow-lg transition-shadow duration-300">
              {news.frontmatter.image && (
                <div class="h-48 overflow-hidden">
                  <img 
                    src={news.frontmatter.image} 
                    alt={news.frontmatter.title}
                    class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
                  />
                </div>
              )}
              <div class="p-6">
                <div class="text-sm text-red-600 font-medium mb-2">
                  {new Date(news.frontmatter.date).getFullYear()}
                </div>
                <h3 class="text-lg font-bold text-gray-900 mb-3">
                  {news.frontmatter.title}
                </h3>
                {news.frontmatter.excerpt && (
                  <p class="text-gray-600 text-sm mb-4">
                    {news.frontmatter.excerpt.substring(0, 120)}...
                  </p>
                )}
                <a 
                  href={`/actualites/${slug}`}
                  class="inline-flex items-center text-red-600 hover:text-red-700 font-semibold text-sm transition-colors"
                >
                  Lire l'article
                  <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                  </svg>
                </a>
              </div>
            </article>
          );
        })}
        
        {recentNews.length === 0 && (
          <div class="col-span-full text-center py-12">
            <p class="text-gray-500 text-lg">Aucune actualité récente disponible.</p>
          </div>
        )}
      </div>
    </div>
  </section>

  <!-- Two Column Section -->
  <section class="py-12 sm:py-16 md:py-20 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 sm:gap-8">
        <!-- Résultats -->
        <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300 touch-manipulation">
          <div class="h-32 sm:h-40 md:h-48 bg-gradient-to-br from-yellow-500 to-yellow-600 flex items-center justify-center">
            <svg class="w-8 h-8 sm:w-10 sm:h-10 md:w-12 md:h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"></path>
            </svg>
          </div>
          <div class="p-4 sm:p-6 md:p-8">
            <h3 class="text-xl sm:text-2xl font-bold text-gray-900 mb-3 sm:mb-4">Résultats</h3>
            <p class="text-gray-600 mb-4 sm:mb-6 text-sm sm:text-base">
              Découvrez les performances de nos coureurs lors des dernières compétitions et courses.
            </p>
            <div class="space-y-2 sm:space-y-3 mb-4 sm:mb-6">
              <div class="flex items-start space-x-3">
                <div class="w-2 h-2 bg-yellow-600 rounded-full mt-2 flex-shrink-0"></div>
                <div>
                  <p class="text-sm font-medium text-gray-900">🏆 3ème place - Pierre Martin</p>
                  <p class="text-xs text-gray-500">Course de côte du Pilat</p>
                </div>
              </div>
              <div class="flex items-start space-x-3">
                <div class="w-2 h-2 bg-yellow-600 rounded-full mt-2 flex-shrink-0"></div>
                <div>
                  <p class="text-sm font-medium text-gray-900">🥈 2ème place - Marie Dubois</p>
                  <p class="text-xs text-gray-500">Cyclo-cross de Firminy</p>
                </div>
              </div>
            </div>
            <a href="/resultats" class="inline-flex items-center text-yellow-600 hover:text-yellow-700 font-semibold text-sm sm:text-base touch-manipulation">
              Voir tous les résultats
              <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>

        <!-- Nous rejoindre -->
        <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300 touch-manipulation">
          <div class="h-32 sm:h-40 md:h-48 bg-gradient-to-br from-gray-800 to-gray-900 flex items-center justify-center">
            <svg class="w-8 h-8 sm:w-10 sm:h-10 md:w-12 md:h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
          <div class="p-4 sm:p-6 md:p-8">
            <h3 class="text-xl sm:text-2xl font-bold text-gray-900 mb-3 sm:mb-4">Nous rejoindre</h3>
            <p class="text-gray-600 mb-4 sm:mb-6 text-sm sm:text-base">
              Envie de faire partie de l'aventure ? Découvrez comment rejoindre notre club et nos conditions d'adhésion.
            </p>
            <div class="space-y-2 sm:space-y-3 mb-4 sm:mb-6">
              <div class="flex items-center space-x-3">
                <svg class="w-4 h-4 sm:w-5 sm:h-5 text-gray-800 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span class="text-sm text-gray-700">Tous niveaux acceptés</span>
              </div>
              <div class="flex items-center space-x-3">
                <svg class="w-4 h-4 sm:w-5 sm:h-5 text-gray-800 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span class="text-sm text-gray-700">Matériel fourni</span>
              </div>
              <div class="flex items-center space-x-3">
                <svg class="w-4 h-4 sm:w-5 sm:h-5 text-gray-800 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span class="text-sm text-gray-700">Encadrement professionnel</span>
              </div>
            </div>
            <a href="/nous-rejoindre" class="inline-flex items-center text-gray-800 hover:text-gray-900 font-semibold text-sm sm:text-base touch-manipulation">
              Rejoindre le club
              <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>