---
import BaseLayout from '../layouts/BaseLayout.astro';

// Récupérer les dernières actualités
const allNewsModules = import.meta.glob('../content/actualites/*.md');
const allNews = await Promise.all(
  Object.entries(allNewsModules).map(async ([path, module]) => {
    const mod = await (module as any)();
    return {
      ...mod,
      file: path
    };
  })
);
const recentNews = allNews
  .sort((a, b) => new Date(b.frontmatter.date).getTime() - new Date(a.frontmatter.date).getTime())
  .slice(0, 3);

// Récupérer les derniers résultats
const allResultsModules = import.meta.glob('../content/resultats/*.md');
const allResults = await Promise.all(
  Object.entries(allResultsModules).map(async ([path, module]) => {
    const mod = await (module as any)();
    return {
      ...mod,
      file: path
    };
  })
);
const recentResults = allResults
  .sort((a, b) => new Date(b.frontmatter.date).getTime() - new Date(a.frontmatter.date).getTime())
  .slice(0, 3);
---

<BaseLayout title="Accueil" description="Étoile Cycliste Ouvrière de Firminy - Découvrez notre club de cyclisme">
  <!-- Hero Section -->
  <section class="relative h-[60vh] sm:h-80 md:h-96 flex items-center justify-center overflow-hidden">
    <!-- Hero Background Image -->
    <div class="absolute inset-0 z-0">
      <img 
        src="/images/hero-ecof.png" 
        alt="Groupe de cyclistes ECOF" 
        class="w-full h-full object-cover hero-image sm:object-center md:object-center"
      />
      <!-- Overlay plus sombre pour mobile pour améliorer la lisibilité -->
      <div class="absolute inset-0 bg-black/40 sm:bg-black/30"></div>
    </div>
    
    <!-- Hero Content -->
    <div class="relative z-10 text-center text-white px-4">
      <h1 class="text-3xl sm:text-4xl md:text-6xl font-bold mb-4 drop-shadow-lg">
        Bienvenue à l'E.C.O.F.
      </h1>
      <p class="text-lg sm:text-xl md:text-2xl font-light drop-shadow-md">
        Étoile Cycliste Ouvrière de Firminy
      </p>
    </div>
    
    <!-- Scroll indicator -->
    <div class="absolute bottom-4 sm:bottom-8 left-1/2 transform -translate-x-1/2 text-white animate-bounce">
      <svg class="w-5 h-5 sm:w-6 sm:h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </section>

  <style>
    /* Optimisation de l'image hero pour différentes tailles d'écran */
    @media (max-width: 640px) {
      /* Mobile : même position que desktop */
      .hero-image {
        object-position: 50% 30% !important;
      }
    }
    
    @media (min-width: 641px) and (max-width: 1024px) {
      /* Tablette : même position que desktop */
      .hero-image {
        object-position: 50% 30% !important;
      }
    }
    
    @media (min-width: 1025px) {
      /* Desktop : position optimale */
      .hero-image {
        object-position: 50% 30% !important;
      }
    }
  </style>

  <!-- Section En bref -->
  <section class="py-12 sm:py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-4">
          <div class="text-center sm:text-left">
            <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-2 sm:mb-0">En bref</h2>
            <p class="text-lg text-gray-600">Les dernières nouvelles de l'E.C.O.F.</p>
          </div>
          <div class="mt-4 sm:mt-0">
            <a 
              href="/actualites" 
              class="inline-flex items-center text-ecof-red hover:text-ecof-yellow font-semibold text-sm bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-full transition-all duration-200"
            >
              Toutes les actualités
              <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {recentNews.map((news) => {
          const slug = news.file.split('/').pop()?.replace('.md', '') || '';
          return (
            <a href={`/actualites/${slug}`} class="block">
              <article class="bg-gray-50 rounded-2xl overflow-hidden hover:shadow-lg transition-all duration-300 cursor-pointer h-full">
                {news.frontmatter.image && (
                  <div class="h-48 overflow-hidden">
                    <img 
                      src={news.frontmatter.image} 
                      alt={news.frontmatter.title}
                      class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
                    />
                  </div>
                )}
                <div class="p-6">
                  <div class="text-sm text-red-600 font-medium mb-2">
                    {new Date(news.frontmatter.date).getFullYear()}
                  </div>
                  <h3 class="text-lg font-bold text-gray-900 mb-3 hover:text-red-600 transition-colors">
                    {news.frontmatter.title}
                  </h3>
                  {news.frontmatter.excerpt && (
                    <p class="text-gray-600 text-sm">
                      {news.frontmatter.excerpt.substring(0, 120)}...
                    </p>
                  )}
                </div>
              </article>
            </a>
          );
        })}
        
        {recentNews.length === 0 && (
          <div class="col-span-full text-center py-12">
            <p class="text-gray-500 text-lg">Aucune actualité récente disponible.</p>
          </div>
        )}
      </div>
    </div>
  </section>

  <!-- Two Column Section -->
  <section class="py-12 sm:py-16 md:py-20 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 sm:gap-8">
        <!-- Résultats -->
        <a href="/resultats" class="block group">
          <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-all duration-300 touch-manipulation cursor-pointer h-full transform group-hover:scale-[1.02]">
            <div class="h-20 sm:h-24 bg-gradient-to-br from-yellow-500 to-yellow-600 group-hover:from-yellow-600 group-hover:to-yellow-700 flex items-center justify-center transition-all duration-300">
              <h3 class="text-lg sm:text-xl font-bold text-white group-hover:scale-105 transition-transform duration-300">Résultats</h3>
            </div>
            <div class="p-4 sm:p-6">
              <p class="text-gray-600 mb-3 sm:mb-4 text-sm group-hover:text-gray-700 transition-colors">
                Découvrez les performances de nos coureurs lors des dernières compétitions et courses.
              </p>
              <div class="space-y-2">
                {recentResults.map((result) => {
                  const participantCount = result.frontmatter.participants?.length || 0;
                  
                  return (
                    <div class="block hover:bg-yellow-50 p-2 rounded-lg transition-colors group-hover:bg-yellow-50">
                      <div class="flex items-start space-x-3">
                        <div class="w-2 h-2 bg-yellow-600 rounded-full mt-2 flex-shrink-0 group-hover:bg-yellow-700 transition-colors"></div>
                        <div>
                          <p class="text-sm font-medium text-gray-900 group-hover:text-yellow-700 transition-colors">
                            🏁 {result.frontmatter.epreuve}
                          </p>
                          <p class="text-xs text-gray-500 group-hover:text-gray-600 transition-colors">
                            {result.frontmatter.lieu ? `${result.frontmatter.lieu} - ` : ''}{new Date(result.frontmatter.date).toLocaleDateString('fr-FR', { 
                              day: 'numeric', 
                              month: 'long', 
                              year: 'numeric' 
                            })} • {participantCount} participant{participantCount > 1 ? 's' : ''} ECOF
                          </p>
                        </div>
                      </div>
                    </div>
                  );
                })}
                
                {recentResults.length === 0 && (
                  <div class="text-center py-4">
                    <p class="text-gray-500 text-sm">Aucun résultat récent disponible.</p>
                  </div>
                )}
              </div>
              
              <!-- Indicateur visuel d'action -->
              <div class="mt-4 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all duration-300 transform translate-y-2 group-hover:translate-y-0">
                <div class="flex items-center text-yellow-600 text-sm font-medium">
                  <span>Voir tous les résultats</span>
                  <svg class="w-4 h-4 ml-2 transform group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                  </svg>
                </div>
              </div>
            </div>
          </div>
        </a>

        <!-- Nous rejoindre -->
        <a href="/nous-rejoindre" class="block group">
          <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-all duration-300 touch-manipulation cursor-pointer h-full transform group-hover:scale-[1.02]">
            <div class="h-20 sm:h-24 bg-gradient-to-br from-gray-800 to-gray-900 group-hover:from-gray-900 group-hover:to-black flex items-center justify-center transition-all duration-300">
              <h3 class="text-lg sm:text-xl font-bold text-white group-hover:scale-105 transition-transform duration-300">Nous rejoindre</h3>
            </div>
            <div class="p-4 sm:p-6">
              <p class="text-gray-600 mb-3 sm:mb-4 text-sm group-hover:text-gray-700 transition-colors">
                Envie de faire partie de l'aventure ? Découvrez comment rejoindre notre club et nos conditions d'adhésion.
              </p>
              <div class="space-y-2">
                <div class="flex items-center space-x-3 group-hover:bg-gray-50 p-2 rounded-lg transition-colors">
                  <svg class="w-4 h-4 text-gray-800 group-hover:text-gray-900 flex-shrink-0 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                  </svg>
                  <span class="text-sm text-gray-700 group-hover:text-gray-800 transition-colors">Tous niveaux acceptés</span>
                </div>
                <div class="flex items-center space-x-3 group-hover:bg-gray-50 p-2 rounded-lg transition-colors">
                  <svg class="w-4 h-4 text-gray-800 group-hover:text-gray-900 flex-shrink-0 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                  </svg>
                  <span class="text-sm text-gray-700 group-hover:text-gray-800 transition-colors">Matériel fourni</span>
                </div>
                <div class="flex items-center space-x-3 group-hover:bg-gray-50 p-2 rounded-lg transition-colors">
                  <svg class="w-4 h-4 text-gray-800 group-hover:text-gray-900 flex-shrink-0 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                  </svg>
                  <span class="text-sm text-gray-700 group-hover:text-gray-800 transition-colors">Encadrement professionnel</span>
                </div>
              </div>
              
              <!-- Indicateur visuel d'action -->
              <div class="mt-4 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-all duration-300 transform translate-y-2 group-hover:translate-y-0">
                <div class="flex items-center text-gray-800 text-sm font-medium">
                  <span>Rejoindre le club</span>
                  <svg class="w-4 h-4 ml-2 transform group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                  </svg>
                </div>
              </div>
            </div>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- Section Ils nous soutiennent -->
<section class="py-12 sm:py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <!-- Texte d’introduction -->
    <div class="text-center mb-12">
      <p class="text-lg text-gray-600 max-w-3xl mx-auto">
        L’ECOF fonctionne grâce à des entreprises et des particuliers qui partagent nos valeurs.
        Leur engagement prend différentes formes, toutes essentielles à la vie du club.
      </p>
    </div>

    <!-- Ceux qui roulent avec nous -->
    <div class="mb-16">
      <h2 class="text-center text-3xl sm:text-4xl font-bold text-gray-900 mb-8">
        Ceux qui roulent avec nous
      </h2>

      <div class="flex flex-wrap justify-center items-center gap-8 sm:gap-12">
        <img src="/images/soutien1.png" alt="Partenaire soutien 1"
          class="h-16 sm:h-20 w-auto object-contain grayscale opacity-70 hover:grayscale-0 hover:opacity-100 transition-all duration-300" />

        <img src="/images/soutien2.png" alt="Partenaire soutien 2"
          class="h-16 sm:h-20 w-auto object-contain grayscale opacity-70 hover:grayscale-0 hover:opacity-100 transition-all duration-300" />
        
          <img src="/images/Logo-2it-solutions-noir.png" alt="Partenaire soutien 3"
          class="h-16 sm:h-20 w-auto object-contain grayscale opacity-70 hover:grayscale-0 hover:opacity-100 transition-all duration-300" />
      </div>

    </div>

    <!-- Ceux qui font vivre le club -->
    <div class="mb-16">
      <h2 class="text-center text-3xl sm:text-4xl font-bold text-gray-900 mb-8">
        Ceux qui font vivre le club
      </h2>

      <div class="flex flex-wrap justify-center items-center gap-8 sm:gap-12">
        <img src="/images/mcsi.jpg" alt="MCSI"
          class="h-16 sm:h-20 w-auto object-contain grayscale opacity-70 hover:grayscale-0 hover:opacity-100 transition-all duration-300" />
      </div>
    </div>

    <!-- Ceux qui soutiennent l'esprit de l'E.C.O.F -->
    <div class="mb-12">
      <h2 class="text-center text-3xl sm:text-4xl font-bold text-gray-900 mb-8">
        Ceux qui soutiennent l’esprit de l’E.C.O.F
      </h2>

      <div class="flex flex-wrap justify-center items-center gap-8 sm:gap-12">
        <img src="/images/securitest.jpg" alt="Securitest"
          class="h-16 sm:h-20 w-auto object-contain grayscale opacity-70 hover:grayscale-0 hover:opacity-100 transition-all duration-300" />
      </div>
    </div>

    <!-- Message de remerciement -->
    <div class="text-center mt-12">
      <p class="text-gray-600 italic">
        Un grand merci à tous nos partenaires pour leur confiance et leur soutien.
      </p>
    </div>

  </div>
</section>

</BaseLayout>